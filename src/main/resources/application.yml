server:
  port: 9000

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fnH
    username: fnH
    password: pass
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update

rsa:
  public-key: classpath:certs/public.pem
  private-key: classpath:certs/private.pem


